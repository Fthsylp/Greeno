@model List<Greeno.Models.Domain.Carousel>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="bg-secondary bg-opacity-10 py-2">
    <div class="container">
        <h1>Show All Carousels - Admin Functionality</h1>
    </div>
</div>

<div>
    <a asp-area="" asp-controller="Carousel" asp-action="Add" >New Carousel</a>
</div>

<div class="container py-5">
    @if (Model.Count != 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                    <th>SubTitle</th>
                    <th>Description</th>
                    <th>Image</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Title</td>
                        <td>@item.SubTitle</td>
                        <td>@item.Description</td>
                        <td>@item.Image</td>
                        <td>
                            <a asp-area="" asp-controller="Carousel" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                            <form asp-area="" asp-controller="Carousel" asp-action="Delete" method="post" style="display:inline;">
                                <input type="hidden" name="Id" value="@item.Id" />
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No Carousel Found!</p>
    }
</div>